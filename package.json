{
  "name": "googlebooks-app",
  "version": "1.0.0",
  "description": "",
  "main": "server/dist/server.js",
  "scripts": {
    "start": "node server/dist/server.js",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm run dev\"",
    "build": "concurrently \"cd server && npm run build\" \"cd client && npm run build\"",
    "render-build": "cd server && npm install && npm run build && cd ../client && npm install --no-optional && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@vitejs/plugin-react": "^4.5.0",
    "concurrently": "^8.2.0",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "bcryptjs": "^3.0.2"
  },
  "engines": {
    "node": "20.x"
  }
}
